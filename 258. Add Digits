// Add Digits
class Solution {
public:
    int addDigits(int num) {
        
        if (num < 10) {return num;}

        while (num >= 10) {
            int first = num / 10;
            int second = num % 10;
            num = first + second;
        }

        return num;
    }
};
